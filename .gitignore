# База данных пользователей — создаётся сервером при первом старте,
# хранится ТОЛЬКО в рантайме (в контейнере), в репозиторий не попадает.
nebosvod.db

# Локальный конфиг (личные локации) — в репозиторий идёт только example
config.json

# Внутренние рабочие документы (промпты задач разработки)
prompt_*.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Логи и временное
*.log
/tmp/
.DS_Store
